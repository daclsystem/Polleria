{
  "name": "polleria",
  "private": true,
  "version": "1.5.0",
  "type": "module",
  "scripts": {
    "dev": "cross-env VITE_APP=web vite",
    "dev:web": "cross-env VITE_APP=web vite",
    "dev:system": "cross-env VITE_APP=system vite",
    "dev:driver": "cross-env VITE_APP=driver vite",
    "dev:cliente": "cross-env VITE_APP=cliente vite",
    "build": "npm run build:all",
    "build:web": "cross-env VITE_APP=web tsc -b && cross-env VITE_APP=web vite build && shx cp deploy/htaccess/web.htaccess dist/web/.htaccess",
    "build:system": "cross-env VITE_APP=system tsc -b && cross-env VITE_APP=system vite build && shx cp deploy/htaccess/system.htaccess dist/system/.htaccess",
    "build:driver": "cross-env VITE_APP=driver tsc -b && cross-env VITE_APP=driver vite build && shx cp deploy/htaccess/driver.htaccess dist/driver/.htaccess",
    "build:cliente": "cross-env VITE_APP=cliente tsc -b && cross-env VITE_APP=cliente vite build && shx cp deploy/htaccess/cliente.htaccess dist/cliente/.htaccess",
    "build:all": "npm run build:web && npm run build:system && npm run build:driver && npm run build:cliente",
    "lint": "oxlint",
    "preview": "cross-env VITE_APP=web vite preview"
  },
  "dependencies": {
    "@types/leaflet": "^1.9.22",
    "leaflet": "^1.9.4",
    "lucide-react": "^1.31.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-leaflet": "^5.0.0",
    "react-router-dom": "^7.18.2",
    "socket.io-client": "^4.8.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^24.13.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "cross-env": "^10.1.0",
    "oxlint": "^1.75.0",
    "shx": "^0.4.0",
    "tailwindcss": "^4.3.3",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
